Файл init.lua - код программы, реализующей proxy-сервер
Файл config.yml - конфигурационный файл, где хранятся настройки для http запроса

Программа написанатаки таким образом, что в случае, если в конфигурационном файле задан host:localhost, то сперва создается http-server с данными хостом и портом.
Для него определяется функция, при помощи которой будет производится ответ сервера на запрос. Это зделано для того, чтобы убедиться, что proxy-сервер может 
направлять запросы не только в хосты в сети (как в yandex.ru), но и в локальные хосты.

Для проверки корректности работы программы менялись параметры в конфигурационном файле. Проверка проводилась на следующих вариантах:

---
proxy:
  port: 8080
  bypass:
    host: yandex.ru
    port: 
    
---
proxy:
  port: 8080
  bypass:
    host: localhost
    port: 9000
    
 При попытке создать proxy-server на порту 80, возникала следующая ошибка: Can't create tcp_server: Permission denied:
 ---
proxy:
  port: 80
  bypass:
    host: yandex.ru
    port: 

